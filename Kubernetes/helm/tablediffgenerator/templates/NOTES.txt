TableDiffGenerator wurde installiert.

{{- if .Values.ingress.enabled }}
Ingress-Zugriff:

{{- range .Values.ingress.hosts }}
  http://{{ .host }}
{{- end }}

Health-Check:

{{- range .Values.ingress.hosts }}
  http://{{ .host }}/healthz
{{- end }}
{{- else if eq .Values.service.type "NodePort" }}
NodePort-Zugriff:

  http://127.0.0.1:{{ .Values.service.nodePort }}

Health-Check:

  http://127.0.0.1:{{ .Values.service.nodePort }}/healthz
{{- else }}
Lokaler Zugriff per Port-Forward:

  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "tablediffgenerator.fullname" . }} 8080:{{ .Values.service.port }}

Danach im Browser öffnen:

  http://127.0.0.1:8080
{{- end }}
