apiVersion: v1
kind: Service
metadata:
  name: {{ include "tablediffgenerator.fullname" . }}
  labels:
    {{- include "tablediffgenerator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tablediffgenerator.selectorLabels" . | nindent 4 }}
