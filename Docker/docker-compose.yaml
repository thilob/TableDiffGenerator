services:
  tablediff-web:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    image: tablediffgenerator-web:local
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      MAX_UPLOAD_SIZE: "33554432"
    restart: unless-stopped
